---
apiVersion: v1
kind: Service
metadata:
  name: inference-worker
  namespace: sparse
  labels:
    app: inference-worker
spec:
  ports:
  - port: 50007
    protocol: TCP
  selector:
    app: inference-worker
