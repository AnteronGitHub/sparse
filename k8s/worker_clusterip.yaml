---
apiVersion: v1
kind: Service
metadata:
  name: sparse-worker
  namespace: sparse
  labels:
    app: sparse-worker
spec:
  ports:
  - port: 50007
    protocol: TCP
  selector:
    app: sparse-worker
