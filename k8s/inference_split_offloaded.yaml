---
apiVersion: v1
kind: Service
metadata:
  name: inference-split-final
  namespace: sparse
  labels:
    app: inference-split-final
spec:
  ports:
  - port: 50008
    protocol: TCP
  selector:
    app: inference-split-final

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inference-split-final
  namespace: sparse
  labels:
    app: inference-split-final
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: inference-split-final
  template:
    metadata:
      labels:
        app: inference-split-final
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: sparse/node
                operator: In
                values:
                - final
                - aio
      containers:
      - name: inference-split-final
        image: split_inference
        imagePullPolicy: IfNotPresent
        env:
        - name: WORKER_LISTEN_ADDRESS
          value: "0.0.0.0"
        - name: WORKER_LISTEN_PORT
          value: "50008"
        command: ["python3"]
        args: ["benchmark.py", "--suite", "offload_final", "--model", "VGG_server"]
        volumeMounts:
        - name: app-code
          mountPath: /app
        - name: sparse-code
          mountPath: /usr/lib/sparse_framework
        - name: sparse-data
          mountPath: /data
        - name: sparse-run
          mountPath: /run/sparse
      volumes:
      - name: app-code
        hostPath:
          path: /opt/sparse/examples/split_inference
      - name: sparse-code
        hostPath:
          path: /opt/sparse/sparse_framework
      - name: sparse-data
        hostPath:
          path: /var/lib/sparse/data
      - name: sparse-run
        hostPath:
          path: /mnt/sparse/run
---
apiVersion: v1
kind: Service
metadata:
  name: inference-split-intermediate
  namespace: sparse
  labels:
    app: inference-split-intermediate
spec:
  ports:
  - port: 50007
    protocol: TCP
  selector:
    app: inference-split-intermediate

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inference-split-intermediate
  namespace: sparse
  labels:
    app: inference-split-intermediate
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: inference-split-intermediate
  template:
    metadata:
      labels:
        app: inference-split-intermediate
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: sparse/node
                operator: In
                values:
                - intermediate
                - aio
      containers:
      - name: inference-split-intermediate
        image: split_inference
        imagePullPolicy: IfNotPresent
        env:
        - name: MASTER_UPSTREAM_HOST
          value: "inference-split-final"
        - name: MASTER_UPSTREAM_PORT
          value: "50008"
        - name: WORKER_LISTEN_ADDRESS
          value: "0.0.0.0"
        command: ["python3"]
        args: ["benchmark.py", "--suite", "offload_intermediate", "--model", "VGG_client"]
        volumeMounts:
        - name: app-code
          mountPath: /app
        - name: sparse-code
          mountPath: /usr/lib/sparse_framework
        - name: sparse-data
          mountPath: /data
        - name: sparse-run
          mountPath: /run/sparse
      volumes:
      - name: app-code
        hostPath:
          path: /opt/sparse/examples/split_inference
      - name: sparse-code
        hostPath:
          path: /opt/sparse/sparse_framework
      - name: sparse-data
        hostPath:
          path: /var/lib/sparse/data
      - name: sparse-run
        hostPath:
          path: /mnt/sparse/run
---
apiVersion: batch/v1
kind: Job
metadata:
  name: inference-split-source
  namespace: sparse
  labels:
    app: inference-split-source
spec:
  template:
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: sparse/node
                operator: In
                values:
                - datasource
                - aio
      containers:
      - name: inference-split-source
        image: split_inference
        imagePullPolicy: IfNotPresent
        env:
        - name: MASTER_UPSTREAM_HOST
          value: "inference-split-intermediate"
        command: ["python3"]
        args: ["benchmark.py", "--suite", "offload_source"]
        volumeMounts:
        - name: app-code
          mountPath: /app
        - name: sparse-code
          mountPath: /usr/lib/sparse_framework
        - name: sparse-data
          mountPath: /data
        - name: sparse-run
          mountPath: /run/sparse
      restartPolicy: Never
      volumes:
      - name: app-code
        hostPath:
          path: /opt/sparse/examples/split_inference
      - name: sparse-code
        hostPath:
          path: /opt/sparse/sparse_framework
      - name: sparse-data
        hostPath:
          path: /var/lib/sparse/data
      - name: sparse-run
        hostPath:
          path: /run/sparse
