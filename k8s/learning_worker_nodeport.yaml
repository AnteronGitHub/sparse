---
apiVersion: v1
kind: Service
metadata:
  name: learning-worker
  namespace: sparse
  labels:
    app: learning-worker
spec:
  type: NodePort
  ports:
  - port: 50007
    targetPort: 50007
    nodePort: $SPARSE_DATASOURCE_DOWNSTREAM_PORT
  selector:
    app: learning-worker

