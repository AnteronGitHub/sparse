---
apiVersion: v1
kind: Service
metadata:
  name: inference-unsplit-final
  namespace: sparse
  labels:
    app: inference-unsplit-final
spec:
  ports:
  - port: 50007
    protocol: TCP
  selector:
    app: inference-unsplit-final

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: inference-unsplit-final
  namespace: sparse
  labels:
    app: inference-unsplit-final
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  selector:
    matchLabels:
      app: inference-unsplit-final
  template:
    metadata:
      labels:
        app: inference-unsplit-final
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: sparse/node
                operator: In
                values:
                - final
                - aio
      containers:
      - name: inference-unsplit-final
        image: split_inference
        imagePullPolicy: IfNotPresent
        env:
        - name: WORKER_LISTEN_ADDRESS
          value: "0.0.0.0"
        command: ["python3"]
        args: ["benchmark.py", "--suite", "offload_final", "--model", "YOLOv3"]
        volumeMounts:
        - name: sparse-data
          mountPath: /data
        - name: sparse-run
          mountPath: /run/sparse
      volumes:
      - name: sparse-data
        hostPath:
          path: /var/lib/sparse/data
      - name: sparse-run
        hostPath:
          path: /mnt/sparse/run
---
apiVersion: batch/v1
kind: Job
metadata:
  name: inference-unsplit-source
  namespace: sparse
  labels:
    app: inference-unsplit-source
spec:
  template:
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: sparse/node
                operator: In
                values:
                - datasource
                - aio
      containers:
      - name: inference-unsplit-source
        image: split_inference
        imagePullPolicy: IfNotPresent
        env:
        - name: MASTER_UPSTREAM_HOST
          value: "inference-unsplit-final"
        command: ["python3"]
        args: ["benchmark.py", "--suite", "offload_source"]
        volumeMounts:
        - name: sparse-data
          mountPath: /data
        - name: sparse-run
          mountPath: /run/sparse
      restartPolicy: Never
      volumes:
      - name: sparse-data
        hostPath:
          path: /var/lib/sparse/data
      - name: sparse-run
        hostPath:
          path: /run/sparse
